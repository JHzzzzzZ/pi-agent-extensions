{
  "name": "pi-extension-loop",
  "private": true,
  "version": "1.9.0",
  "type": "module",
  "description": "loop — Pi 定时任务扩展（/loop：固定间隔 / 每天定时 / 每日窗口循环 + 一次性提醒 + --bg 后台 agent 模式（可选模型指定）+ agent 工具；v1.8.0：后台轮次允许重叠（不设并发上限，轮次历史走 loop-run-v1 条目，轮次行带会话 id）｜v1.7.0：单轮超时 30m → 3h（大于 headless auto-drain 上限）｜v1.6.1：修复后台调度丢失任务模型（startBgRun 透传 task.model）｜v1.6.0：命令面冒号化（/loop:list|pause|resume|delete|clear 独立静态命令）｜v1.5.0：状态条对齐墙钟秒节拍（aligned-ticker.ts，契约 docs/cross/status-bar.md）｜v1.9.0：widget 排序带——倒计时 widget 并入单键 widget-band 并保序（#28））",
  "engines": {
    "node": ">=22.18"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "test": "node --test \"test/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@types/node": "^22.0.0",
    "typebox": "^1.3.10",
    "typescript": "^5.8.0"
  }
}
