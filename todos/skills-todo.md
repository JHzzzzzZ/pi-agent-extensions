# TODO（用户级 · skills/ 全局技能）

> 非插件条目：`~/.pi/agent/skills/` 下的全局技能，不对应任何 `pi.extensions` 插件，单独建档。

- [x] 新建 `todo-triage` 开工盘点 skill：开工时扫描 `todos/*-todo.md` 与 git worktree，把待办判为进行中/未领取/待决策/被阻塞/搁置，核对 worktree↔todo 映射（合并遗留、孤儿目录、无工作台的 processing），输出推荐领取优先级与下一步动作。（完成 2026-09-11 @ `~/.pi/agent/skills/todo-triage/`：`SKILL.md` + `scripts/survey.sh` 只读事实收集器——5 步门（收集→归类→worktree 健康度→排序报告→领取动作）+ 完成标准；实测覆盖 16 个 todo 文件/25 条 pending/全部 worktree 状态与孤儿目录，含从 worktree 内调用自动切回主工作区、非仓库根报错路径）
- [x] 新建 `wrap-up` 开发任务收尾 skill：任务结束时触发（用户调用或 agent 自主触发），核对测试完备（单测/集成/typecheck 全绿）+ 文档同步（README/AGENTS/package.json/todos/docs），有缺口就地补齐，最后产出简短收尾报告（改动摘要 + 验收方式 + 功能用法）。
- [x] 新建 `todo-add` 需求受理 skill：把一条需求路由到唯一落点——现有插件 → `todos/<插件名>-todo.md`；未实现插件 → 新建 `todos/<插件名>-todo.md`；通用领域/涉及多插件 → `todos/general-todo.md`；含查重、条目写法与路由报告，只登记不标 processing；同步 AGENTS.md 红线 2 路由与新建 general-todo.md。（完成 2026-09-11 @ `~/.pi/agent/skills/todo-add/`：SKILL.md 四步门（整理查重→路由→写入→报告）+ 完成标准，现有插件清单以根 `package.json` `pi.extensions` 为权威；`todos/general-todo.md` 建档（非插件通用/跨插件落点）；AGENTS.md 红线 2/7 同步；`todo-triage` 已知坑同步；技能 README 索引已加）
- [x] 新建 todo-cli skill（~/.pi/agent/skills/todo-cli/）并让 todo-add / todo-triage 的机械步骤改走仓库 CLI（用户 2026-09-11，配合 todo-cli 从 Pi 插件转 CLI）：skill 教 agent 用 node tools/todo.mjs 执行登记/领取/完成/盘点/triage/lint，含 scripts/todo.sh 包装器（自动定位仓库根）；todo-add 写入改走 add（跨文件查重）、todo-triage 领取改走 claim --branch；skills README 索引同步。验收：skill 命令面与 CLI 实际行为一致、触发词覆盖各动作。（完成 ~/.pi/agent/skills/todo-cli/（SKILL.md + scripts/todo.sh 包装器，实测 summary/lint exit 0 ；todo-add 写入改走 add（跨文件查重 、todo-triage 领取改走 claim --branch、README 索引同步；命令面与合并后 tools/todo.mjs（merge 4951d9a 逐条核对一致）
