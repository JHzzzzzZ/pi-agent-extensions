- [x] 根 README 为每个插件增加效果示意图
- [x] 增加 opencode go（Muse Spark 等模型）的额度显示适配器（实测端点 /zen/go/v1/usage；限额窗口重置时间跟随命中限额的窗口）
- [x] 状态条时钟统一与排序（跨插件）：footer 状态键 → `20:provider-quota`（5 分钟轮询节奏不变）；契约 `docs/cross/status-bar.md`（完成 2026-09-10 @ feat/status-clock b9d397e，provider-quota 25 测试绿）。
- [x] footer 多源状态挤占优化（跨插件）：多插件同时写 footer 时，宿主按 key 排序后把各段单空格拼一行并整行右截断（宿主 `footer.js`：`sortedStatuses.join(" ")` + `truncateToWidth`）——段间无分隔、按字符硬切、无优先级。真机截图（2026-09-11）：goal + provider-quota + pwr 同屏时 pwr 段被截成 `workflows: 2 activ...`，stream-token-speed 在场会被整段截没。涉及全部 5 个 footer 写入者：goal(`10:goal`)、provider-quota(`20:provider-quota`)、pwr(`30:pwr`)、solo-mode(`40:solo-mode`)、stream-token-speed(`50:stream-token-speed`)；本插件段 `GO 5h X% 周 Y% 月 Z% → 09-11 00:44 (4h38m)`（约 35 字符，重置后缀占比大）位于排序带 20。优化方向待定：(a) 各插件文本瘦身/次要信息分时轮播（本插件重置后缀可缩短或仅在限额时显示、百分比可省 `%`）；(b) 段间可见分隔/短标签契约（统一前缀图标或 `│`），提升可辨性；(c) 截断改按段/按优先级而非尾部硬切（可能需宿主 `footer.js` 补丁，沿 `docs/pi-widget-order-patch.md` 先例）；(d) 低优先级状态迁往编辑器上方 widget（该栈顺序另有契约）。约束：不引入跨插件通信（`docs/cross/status-bar.md` 非目标）；5 分钟轮询节奏不变；完成时同步该契约卡 + 涉及插件 README/测试。跨插件需求，已在 goal / pwr / solo-mode / stream-token-speed todo 同步登记。（完成 2026-09-14 @ feat/status-footer-lines：方案 (c) 落地——宿主 footer 改为各扩展状态独占一行 + 单段超宽 `wrapTextWithAnsi` 续行（不截断、信息零损失），插件零改动、文案全保留；补丁文档 `docs/pi-footer-status-patch.md`（升级 pi 后重打 / 还原 `footer.js.bak-footer-status` 回滚），契约卡 + INDEX + 五卡 + README/AGENTS 已同步；仓库侧全量回归与 typecheck 绿。）
- [ ] 上游根修（route A）：pi 宿主 `InteractiveMode.setExtensionWidget` 保序 bug——同 key 更新先 `Map.delete` 再 `Map.set`，把 widget 挪到所在栈底部；多个周期刷新的 widget 因此逐秒换位。本插件主要写 footer（按 key 排序，不受插入序影响），同属宿主根修受益面。需求：把最小复现 + 源码级 diff（`keepPosition`：目标栈已存在 key 时原地 `Map.set`、不清除位置；换 placement / 清除仍从对应栈删除）提交上游 `github.com/earendil-works/pi`（目标 `packages/coding-agent/src/modes/interactive/interactive-mode.ts`）；上游修复发布后撤本地补丁并回归状态条顺序契约。跨插件需求，已在 run-timer / loop / goal / pwr / provider-quota / solo-mode / stream-token-speed / agent-team 的 todo 同步登记。（processing 2026-09-10 @ route A：材料已备，待提交上游）
