# solo-mode-todo

- [x] 提供一个开关或快捷键激活 pi agent 的 solo 模式（完成 2026-09-11 @ 9e2c15d / feat-solo-mode-approval-gate）
  - 交付：新增 `solo-mode/` 扩展（11 测试）+ pwr 410 / opencode-bridge 113 / deep-init 37 全绿 + 三处 typecheck 零错误；文档同步（`docs/extensions/solo-mode.md`、`docs/cross/solo-approval-gate.md`、docs/INDEX、三张扩展卡 last verified）与版本 bump（根 2.16.0 / pwr 2.5.0 / bridge 1.5.0 / deep-init 1.2.0）。
  - 语义定案（2026-09-11，用户确认）：solo 模式 ＝ **无需审批，危险操作自动批准**（审批摩擦豁免），非"禁用委派"亦非"精简运行"。
  - v1 形态：独立卫星扩展 `solo-mode/`，仅 `/solo` 命令（`/solo` 切换、`/solo on|off|status`，未知参数提示用法）；不做快捷键。
  - v1 范围（审批摩擦门）：PWR 批准卡（自动按 once）、opencode-bridge sync / 端口切换 / restore 确认（restore 自动选最新备份）、deep-init `--create-new` 二次确认。**不含**误触保护类确认（agent-team viewer `D` 停止、`/team:clear`、pwr 删除选择）。
  - 生命周期：仅当前会话（`/reload`、`/new`、`/resume`、`/fork`、进程退出即复位）；开启需一次确认，无 UI 环境拒绝激活。
  - 跨扩展契约：状态文件 `${PI_SOLO_MODE_FILE:-~/.pi/agent/solo-mode.json}` = `{pid, activatedAt}`，读者校验 `pid === process.pid`（fail-closed），故子 pi 进程不继承 solo；契约卡 `docs/cross/solo-approval-gate.md`。
  - 非目标：不禁用委派工具、不自动处理 pi `project_trust` 提示、不做跨会话持久化。
  - 实现注意：与其它卫星扩展形态一致（目录 + `index.ts` 入口，复制进 `extensions/` 后 `/reload` 生效）；TUI 写入前 `ctx.hasUI` 守卫；新增插件需在同一变更内同步 docs/ 卡片、README、根 package.json 与本 todo 文件。
- [x] 状态条时钟统一与排序（跨插件）：footer 状态键 → `40:solo-mode`（静态写入不变）；契约 `docs/cross/status-bar.md`（完成 2026-09-10 @ feat/status-clock b9d397e，solo-mode 12 测试绿）。
- [ ] footer 多源状态挤占优化（跨插件）：多插件同时写 footer 时，宿主按 key 排序后把各段单空格拼一行并整行右截断（宿主 `footer.js`：`sortedStatuses.join(" ")` + `truncateToWidth`）——段间无分隔、按字符硬切、无优先级。真机截图（2026-09-11）：goal + provider-quota + pwr 同屏时 pwr 段被截成 `workflows: 2 activ...`，stream-token-speed 在场会被整段截没。涉及全部 5 个 footer 写入者：goal(`10:goal`)、provider-quota(`20:provider-quota`)、pwr(`30:pwr`)、solo-mode(`40:solo-mode`)、stream-token-speed(`50:stream-token-speed`)；本插件段 `⚡ solo`（`SOLO_STATUS_TEXT`，排序带 40）最短、几乎不占宽，主要受段间分隔/排序契约影响（优化时可加统一分隔符或保持现状）。优化方向待定：(a) 各插件文本瘦身/次要信息分时轮播（本插件无需瘦身）；(b) 段间可见分隔/短标签契约（统一前缀图标或 `│`），提升可辨性；(c) 截断改按段/按优先级而非尾部硬切（可能需宿主 `footer.js` 补丁；宿主补丁现已禁用，见 `docs/pi-widget-order-issue.md`）；(d) 低优先级状态迁往编辑器上方 widget（本插件静态短文本不建议迁）。约束：不引入跨插件通信（`docs/cross/status-bar.md` 非目标）；静态写入不变；完成时同步该契约卡 + 涉及插件 README/测试。跨插件需求，已在 goal / provider-quota / pwr / stream-token-speed todo 同步登记。（2026-09-14：宿主补丁方案已作废并完全还原——「仓库外禁改，除非人工批准」规则见 AGENTS.md「仓库边界」；替代方案：新增 status-footer 插件用公开 API `ctx.ui.setFooter` 接管 footer、扩展状态逐行 + 单段超宽续行，待人工确认后实施。）
- [ ] 支持 `pi --solo` 启动参数默认开启 solo 模式（未领取）
  - 需求：启动时带 `--solo` 即自动激活免审批模式，免去进 TUI 后再敲 `/solo`；同时解决无头场景（`-p` / `--mode json` 无 UI，当前 `/solo on` 因拿不到确认框而拒绝激活）用不了 solo 的问题。
  - 方案（待细化）：solo-mode 用 `pi.registerFlag("solo", { description, type: "boolean", default: false })` 注册；`session_start`（reason `"startup"`）读 `pi.getFlag("solo")`，命中即直接激活——CLI 显式意图不经 `ctx.ui.confirm`、允许 `hasUI === false`，仍走既有路径写状态文件 + 状态条（有 UI 时）+ notify。
  - 待定：是否同时支持 settings.json 持久配置（免得每次敲）；与 `/solo off` 的交互（flag 仅启动时生效一次，之后以命令状态为准）；`-p` / json 模式无状态条时的可见性（只靠状态文件 + 首条 notify）。
  - 安全边界：子 pi 进程（PWR runner / agent-team 成员 / loop `--bg`）argv 不带该 flag，且状态文件仍为 pid 作用域 → 子进程不继承 solo，跨扩展契约不变。
  - 同步要求：实现时更新 `docs/extensions/solo-mode.md`（职责/文件地图/数据流 + `last verified`）、根 README solo-mode 小节、本 todo 文件；补测试（flag 命中激活 / 未命中不动 / 无 UI 仍激活 / reload 后复位 / 与 `/solo off` 交互）。
- [x] 命令面改冒号形式（跨插件，全量任务一部分）：`/solo on|off|status` → `/solo:on|:off|:status`；bare `/solo` 切换保留。全量清单与待定项见 `todos/commands-colon-todo.md`。（完成 2026-09-14 @ merge 134a3d6：solo-mode v1.2.0——裸 `/solo` 空参仍是一键切换（与习惯一致），`on`/`off`/`status` 改为独立冒号命令，旧空格词只提示改名、绝不执行（避免 `/solo on` 被误当反向切换）；`parseSoloCommand` 收敛为 toggle/usage。14 测试绿；真实 pi loader 冒烟通过）
- [ ] 上游根修（route A）：pi 宿主 `InteractiveMode.setExtensionWidget` 保序 bug——同 key 更新先 `Map.delete` 再 `Map.set`，把 widget 挪到所在栈底部；多个周期刷新的 widget 因此逐秒换位。本插件主要写 footer（按 key 排序，不受插入序影响），同属宿主根修受益面。需求：把最小复现 + 源码级 diff（`keepPosition`：目标栈已存在 key 时原地 `Map.set`、不清除位置；换 placement / 清除仍从对应栈删除）提交上游 `github.com/earendil-works/pi`（目标 `packages/coding-agent/src/modes/interactive/interactive-mode.ts`）；上游修复发布后撤本地补丁并回归状态条顺序契约。跨插件需求，已在 run-timer / loop / goal / pwr / provider-quota / solo-mode / stream-token-speed / agent-team 的 todo 同步登记。（processing 2026-09-10 @ route A：材料已备，待提交上游）
