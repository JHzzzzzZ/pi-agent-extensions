- [x] 状态条时钟统一与排序：run-timer 与 loop 等多个插件各自写状态条（如 `⏰ loop 2 个任务 · 下次 12h39m` 与计时行），不断刷新导致换行/重排、相互挤占。期望：不同插件共用同一时间钟（统一刷新节拍/合并渲染），且状态条各段有明确的排序先后约定。涉及 run-timer + loop（及后续有状态条的插件）。（完成 2026-09-10 @ feat/status-clock b9d397e：run-timer/loop/goal/agent-team 各带一份 `aligned-ticker.ts` 对齐墙钟秒节拍（自校正/stop 幂等/异常吞掉），tick 驱动写入加文本指纹跳过；footer 键双位排序带 `10:goal`/`20:provider-quota`/`30:pwr`/`40:solo-mode`/`50:stream-token-speed`；上方 widget 栈顺序 `pwr-runs→run-timer→loop` 由根 `test/status-bar-contract.test.ts`（`npm run test:contract`）锁定；契约卡 `docs/cross/status-bar.md`。回归：run-timer 59 / loop 192 / goal 60 / agent-team 288 / pwr 417 全绿 + typecheck）
- [x] 根 README 为每个插件增加效果示意图
