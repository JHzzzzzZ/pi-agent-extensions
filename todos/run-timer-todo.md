- [ ] 状态条时钟统一与排序：run-timer 与 loop 等多个插件各自写状态条（如 `⏰ loop 2 个任务 · 下次 12h39m` 与计时行），不断刷新导致换行/重排、相互挤占。期望：不同插件共用同一时间钟（统一刷新节拍/合并渲染），且状态条各段有明确的排序先后约定。涉及 run-timer + loop（及后续有状态条的插件）。
- [x] 根 README 为每个插件增加效果示意图
