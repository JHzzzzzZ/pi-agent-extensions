# todo-cli-todo#13 test:todo 偶发失败：先可观测、后定性

> 领取：2026-09-15（本会话）· 对齐访谈：2026-09-15 grill-with-docs

## 意图

2026-09-14 实测：并发负载下（同仓另一会话在跑 todo CLI 写操作）`test:todo` 首跑 42/44、fail 2（疑为 concurrency.test.ts 真子进程用例）；独立复跑两次均 44/44 全绿。现状失败时无现场（用例名、stderr、时间线），无法判定是锁等待 / 超时预算问题还是测试自身竞态——盲目放宽预算或标串行都可能掩盖真问题。

## 范围

口径来自访谈 Q5：两步走。第一步（本单范围）——**只加可观测性，不改任何测试语义**：

1. 并发相关用例失败时把现场落盘到 `os.tmpdir()` 下带时间戳的文件：用例名、完整 stderr、关键时间线（子进程起止 / 锁等待 / 超时点）；断言失败信息携带现场文件路径。
2. 注入诊断负载复现：模拟「同仓另一会话并发写」场景（真子进程 + 并发锁竞争），跑多轮定位可复现条件。
3. 定性结论记入 `docs/tools/todo-cli.md` 卡片已知坑（或 docs/incidents.md，视严重度）。

第二步——**取决于第一步发现**（放宽等待预算 / 标串行 / 修竞态三选一），留待后续轮次另对齐，不在本单范围。

明确不做什么：

- 不改任何测试的语义与预算、不动 concurrency 用例的断言、不做批量测试重构。

## 验收标准

1. 若复现：失败轮次产出现场文件，含用例名与 stderr，足以人工判读。
2. 若多轮（≥5）未自然复现：用人为锁竞争注入验证诊断链路可捕获（否则诊断是摆设）。
3. 诊断代码零行为改动：未失败路径的测试输出与现版本一致；全量 `test:todo` 全绿。

## 人工确认

确认人：用户 · 日期：2026-09-15 · 方式：本会话 grill-with-docs，Q5「两步走（先可观测后定性）」采纳推荐；第二步方案留待第一步发现后另议。用户答复「当前没问题」终确认。
