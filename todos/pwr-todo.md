- [x] 根 README 为每个插件增加效果示意图
- [x] devDependencies 安全升级：@earendil-works/pi-coding-agent 等 ^0.83.0 → ^0.85.1，修复 undici/brace-expansion 高危漏洞
- [x] pwr 接线 `session_shutdown` 钩子调用 `runtime.shutdown()`：pi 0.85.1 已提供该钩子（/new、/resume、/fork、/clone、exit 都触发，见宿主 docs/extensions.md 生命周期图），AGENTS.md 已知怪癖的前提已不成立。接线后 FIFO 队列/缓存/调度器可在会话切换与退出时清理，不残留跨会话状态。（完成 2026-09-10：TDD 新增生命周期接线测试；发现并修复配套缺口——runtime 是模块级单例，shutdown 后无复位路径，新增 `revive()` 在 session_start 复位 SESSION_SHUTDOWN 闩锁，否则 /new 一次后 start() 永久抛错；`RuntimeAdapter` 增加可选 `shutdown?()/revive?()` 契约。406 测试 + typecheck 绿；agent-team/loop/goal/provider-quota/run-timer 均已接此钩子，pwr 补齐为最后一个）
- [x] 命令风格统一（跨插件）：冒号命名空间式命令面（`/workflows:list|view|open|pause|resume|stop|restart|save|saved|script|approve|help` 及 `/workflow-delete`、`/pwr-model`）与统一基准（loop 的子命令式）不一致。跨插件需求，已在 agent-team-todo.md 同步登记。（完成 2026-09-10 @ merge a03e525：pwr v2.6.0——13 个 `/workflows:*` 合并为单 `/workflows` + 子命令；`/workflow run|delete|model` 子命令；动态 `/workflow:<name>` 注册退役，saved 调用运行时读盘。416 测试 + typecheck 绿）
- [x] 采纳 solo 审批门（跨插件，solo-mode 条目一部分）：`/solo` 开启时 workflow_validate 不弹批准卡（按 once 自动批准）、workflow_start 强制降级 once、已保存命令 approveSavedCommand 返回 once；绝不写 remembered 批准。（完成 2026-09-11 @ 9e2c15d：`pwr/src/solo-gate.ts` + index.ts 三处接线 + entry.test 集成用例，410 测试全绿）
- [x] cmd-style 合并后的 solo 互操作复核（完成 2026-09-11 @ a03e525）：① `pwr/test/entry.test.ts` 的“已保存命令 solo 自动批准”用例已改走统一命令面（`commands.get("workflow")` + args `run solo-saved`）；② `approveSavedCommand` solo 分支仍在 `runSavedWorkflow` 调用链上；③ 冲突文件解决结果保住了 solo-mode 内容（卡片/INDEX 行/solo-gate 接线/版本）。合并后全套：pwr 416、agent-team 279、opencode-bridge 114、deep-init 37、solo-mode 11 全绿 + 三处 typecheck 零错误。
- [ ] pwr 的 TUI 与 pi-agent 同步，代码层级对齐（2026-09-11 进展：A1–A4 均已完成，仅剩 A5「结果页 Markdown」缓办，见下）

  pwr 自带无宿主 TUI 层（`src/ui/`：`MemoryRunStore` / `views.ts` / 批准卡 / entry 渲染器 / widget-status），只有 `renderer.ts` 直接碰宿主 `pi-tui`（`Box`/`Text`）；宿主侧一改渲染或主题契约，pwr 的显示就可能悄悄跑偏。对照宿主 pi-agent/pi-tui 的实现逐项对齐，差异只留 PWR 特有语义。
  - [x] 建对照矩阵：pwr 侧（`src/ui/*` + 快捷键注册表 `keybindings.ts`）逐项对应到 pi-tui/host 侧版本与用法，矩阵落盘（pwr/DELIVERY.md 小节或 AGENTS.md）。（完成 2026-09-10 @ feat/pwr-tui-sync-matrix：落盘 `pwr/docs/tui-sync.md`——沿 agent-team/docs/tui-sync.md 先例选独立文件而非 DELIVERY 小节（DELIVERY 为受损的版本历史文档，矩阵需要独立复核生命周期）；含文件映射/对齐维度/行动项 A1-A5（A1 按键匹配换 matchesKey 有 kitty 协议正确性风险，A2 text.ts 换宿主 truncateToWidth/wrapTextWithAnsi 删自写轮子）/宿主升级复核流程，基线 pi-tui 0.85.1）
  - [x] 对齐粒度到代码层：`Box`/`Text` 用法、主题取色（`theme.fg`）、widget/status 写入契约、entry 渲染器形态、快捷键注册方式；宿主已有现成组件的不自造轮子。（完成 2026-09-11 @ merge 6021122：`/workflows view` 重做为 fleet/agent-team 同款分栏外壳（roster + detail、fleet 键位/几何/750ms 指纹门控、`D` 两步停止）；tui-sync 矩阵 A2（删自写 `src/ui/text.ts` 改宿主 truncateToWidth/wrapTextWithAnsi/visibleWidth）、A3（750ms）、A4（VIEWER_OVERLAY_OPTIONS + fleet 公式）完成；A5（结果页 Markdown）仍缓办。436 测试 + typecheck 绿；新增真实宿主测试 `tests/ui-viewer-host.test.ts` 防 overlay 堆叠）
  - [x] 约束不变：`@earendil-works/pi-tui` 只作 devDependencies（宿主运行时解析）；测试保持结构 fake，唯一例外 `tests/ui-viewer-host.test.ts`（真实 `TuiMainScreen` + 假终端，堆叠类 bug 只在真实合成/diff 路径存在，文件头已注明破例动机）；每次宿主升级即复核矩阵。（完成 2026-09-11 @ 6021122）
- [x] 状态条时钟统一与排序（跨插件）：footer 状态键 `pwr` → `30:pwr`（widget 键 `pwr-runs` 不变；推送式刷新不接 ticker）；契约 `docs/cross/status-bar.md`（完成 2026-09-10 @ feat/status-clock b9d397e，pwr 417 测试 + typecheck 绿）。
- [ ] `/workflow` 与 `/workflows` 命令归一：单数=定义面（`<任务>` 生成、`run`/`delete`/`model`），复数=运行观察控制面（`list|view|open|pause|resume|stop|restart|save|saved|script|approve|help`）；名字只差一个 `s`，`/workflows save`（把 run 存为工作流）与 `/workflow run`（跑已保存工作流）极易混，且只有复数根有 `help`/补全。方向待定：① 全并入 `/workflow`（`/workflow list|view|pause…`）彻底消歧；② 保留两根但补 `/workflow help`、统一 help/补全/保留词约定与文案。约束：沿命令风格统一先例直接替换不留旧别名；需先划清 saved 工作流生命周期各动作归哪侧。
- [ ] footer 多源状态挤占优化（跨插件）：多插件同时写 footer 时，宿主按 key 排序后把各段单空格拼一行并整行右截断（宿主 `footer.js`：`sortedStatuses.join(" ")` + `truncateToWidth`）——段间无分隔、按字符硬切、无优先级。真机截图（2026-09-11）：goal + provider-quota + pwr 同屏时本插件段被截成 `workflows: 2 activ...`（实证受害者），stream-token-speed 在场会被整段截没。涉及全部 5 个 footer 写入者：goal(`10:goal`)、provider-quota(`20:provider-quota`)、pwr(`30:pwr`)、solo-mode(`40:solo-mode`)、stream-token-speed(`50:stream-token-speed`)；本插件段 `workflows: N active (<名> <状态>), N finished`（`pwr/src/ui/renderer.ts` `runStatusText`，排序带 30）。优化方向待定：(a) 各插件文本瘦身/次要信息分时轮播（本插件 `N finished`、脚本名/状态段可缩短或省略）；(b) 段间可见分隔/短标签契约（统一前缀图标或 `│`），提升可辨性；(c) 截断改按段/按优先级而非尾部硬切（可能需宿主 `footer.js` 补丁；宿主补丁现已禁用，见 `docs/pi-widget-order-issue.md`）；(d) 低优先级状态迁往编辑器上方 widget（本插件已有 `pwr-runs` widget，footer 可只留计数）。约束：不引入跨插件通信（`docs/cross/status-bar.md` 非目标）；推送式刷新节奏不变；完成时同步该契约卡 + 涉及插件 README/测试。跨插件需求，已在 goal / provider-quota / solo-mode / stream-token-speed todo 同步登记。（2026-09-14：宿主补丁方案已作废并完全还原——「仓库外禁改，除非人工批准」规则见 AGENTS.md「仓库边界」；替代方案：新增 status-footer 插件用公开 API `ctx.ui.setFooter` 接管 footer、扩展状态逐行 + 单段超宽续行，待人工确认后实施。）
- [x] 命令面改冒号形式（跨插件，全量任务一部分）：`/workflow run|delete|model` → `/workflow:run|:delete|:model`；`/workflows list|view|…|help` → `/workflows:list` 等；生成入口 `/workflow <任务>` 保持不变。全量清单与待定项见 `todos/commands-colon-todo.md`。（完成 2026-09-14 @ merge 134a3d6：pwr v2.8.0——`/workflow:run|:delete|:model` 与 `/workflows:list|:view|:open|:pause|:resume|:stop|:restart|:save|:saved|:script|:approve|:help` 独立静态注册（共 17 条命令）；裸 `/workflows` 保留无参=列表/`<runId>`=详情/`--filter`/`help`，裸 `/workflow` 空参=用法、其余文本=生成；旧空格子命令只提示改名（防止 `run <名>` 误生成）。`parseWorkflowCommandRoute`/`parseWorkflowsCommand` 退役；438 测试 + typecheck 绿；真实 pi loader 冒烟通过）
- [ ] 上游根修（route A）：pi 宿主 `InteractiveMode.setExtensionWidget` 保序 bug——同 key 更新先 `Map.delete` 再 `Map.set`，把 widget 挪到所在栈底部；多个周期刷新的 widget 因此逐秒换位。本插件 `pwr-runs` widget 位于上方栈；footer 段顺序另受 key 排序带约束（见 `docs/cross/status-bar.md`）。需求：把最小复现 + 源码级 diff（`keepPosition`：目标栈已存在 key 时原地 `Map.set`、不清除位置；换 placement / 清除仍从对应栈删除）提交上游 `github.com/earendil-works/pi`（目标 `packages/coding-agent/src/modes/interactive/interactive-mode.ts`）；上游修复发布后撤本地补丁并回归状态条顺序契约。跨插件需求，已在 run-timer / loop / goal / pwr / provider-quota / solo-mode / stream-token-speed / agent-team 的 todo 同步登记。（processing 2026-09-10 @ route A：材料已备，待提交上游）
