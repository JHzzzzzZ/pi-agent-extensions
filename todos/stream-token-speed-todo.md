- [x] 根 README 为每个插件增加效果示意图
- [x] 状态条时钟统一与排序（跨插件）：footer 状态键 → `50:stream-token-speed`（调用点统一用 `STATUS_KEY` 常量；250ms 流式节流不变）；契约 `docs/cross/status-bar.md`（完成 2026-09-10 @ feat/status-clock b9d397e，stream-token-speed 43 测试绿）。
- [ ] footer 多源状态挤占优化（跨插件）：多插件同时写 footer 时，宿主按 key 排序后把各段单空格拼一行并整行右截断（宿主 `footer.js`：`sortedStatuses.join(" ")` + `truncateToWidth`）——段间无分隔、按字符硬切、无优先级。真机截图（2026-09-11）：goal + provider-quota + pwr 同屏时 pwr 段被截成 `workflows: 2 activ...`；本插件段最长（`TTFT 1200 ms｜最后 45.2 tok/s｜平均 38.1 tok/s`，约 40 字符）且排序带 50 最末，是整段被截没的头号受害者。涉及全部 5 个 footer 写入者：goal(`10:goal`)、provider-quota(`20:provider-quota`)、pwr(`30:pwr`)、solo-mode(`40:solo-mode`)、stream-token-speed(`50:stream-token-speed`)。优化方向待定：(a) 本插件文本瘦身/次要信息分时轮播（如默认只留 `TTFT+瞬时`、平均段轮播或省略、` tok/s` 后缀缩为 `t/s`）；(b) 段间可见分隔/短标签契约（统一前缀图标或 `│`），提升可辨性；(c) 截断改按段/按优先级而非尾部硬切（可能需宿主 `footer.js` 补丁，沿 `docs/pi-widget-order-patch.md` 先例）；(d) 本插件状态迁往编辑器上方 widget（避免与其余 footer 段争宽）。约束：不引入跨插件通信（`docs/cross/status-bar.md` 非目标）；250ms 流式节流与指纹跳过不变；完成时同步该契约卡 + 涉及插件 README/测试。跨插件需求，已在 goal / provider-quota / pwr / solo-mode todo 同步登记。细节待定，未领取。
