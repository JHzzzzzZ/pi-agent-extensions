# TODO（通用领域 · 跨插件）

> 非插件条目：不属于任何现有或规划插件的通用功能、工作流改进，以及涉及多个插件的需求统一登记在此；由 `todo-add` skill 路由写入。技能类需求见 `skills-todo.md`，文档知识库类见 `docs-todo.md`。

- [x] 全新安装加载冒烟工具（GOAL.md §2「别人这把」：外部用户从零装到跑通）：把根 `pi.extensions` 清单里的全部扩展复制到全新临时 `PI_CODING_AGENT_DIR`（不碰用户真实配置），拉起真实 `pi --mode rpc` 进程，核对每个扩展的命令注册与启动期 widget/status 写入；失败时保留临时目录并打印问题清单。（完成 2026-09-11 @ feat/install-smoke：`tools/install-smoke.mjs`（纯校验函数导出 + CLI）+ `test/install-smoke.test.ts` 8 个单测；端到端实测 12/12 扩展、50 条命令、9 个 TUI 键全部来自本次临时安装目录；README「安装自检」节 + AGENTS 开发命令 + 根 `smoke`/`test:smoke` npm 脚本）
- [x] 全新安装深度冒烟（GOAL.md §2「别人这把」：从零装到跑通**核心流程**）：加载冒烟只证明扩展被注册，不证明工具真能被模型用起来。加 `--task` opt-in：同一临时安装目录内复制用户 `auth.json`（仅临时目录使用、结束即删、不打印内容），拉起 `pi --mode json -p --tools <工具>` 跑一次真实模型调用，以事件流 `tool_execution_start/end` 为判据（模型自由文本不算），默认被测工具 `loop_list`（只读、结果确定），模型默认取配置 `defaultProvider/defaultModel`、可 `--model` 覆盖；失败保留临时目录排查、成功自动清理。（完成 2026-09-11 @ feat/install-deep-smoke：`buildDeepArgs`/`parseJsonEvents`/`checkDeepRun`/`deepModelFromSettings` 纯函数 + 6 个新单测（共 14）；端到端实测 12/12 扩展 + 模型 `opencode-go/deepseek-flash` 调用全新安装的 `loop_list` 成功，全程 ~10s；README/AGENTS/incidents 同步）
- [ ] 远端发布线卫生（GOAL.md §2「别人这把」，需人工决策）：远端默认分支 `master` 停在历史发布线 v1.0.0（仅 5 个扩展，2026-09-06 后未动），本地 `dev-laptop` 又领先 `origin/dev-laptop` 45 个提交（2026-09-11 实测）——外部用户按 README 安装会拿到旧包（已临时固定 `@dev-laptop` 兜底）。需要：① `git push origin dev-laptop` 同步远端；② 决定默认分支（把 `dev-laptop` 设为默认，或让 master 快进到 dev-laptop）；③ 发布策略（里程碑打 tag，README 可固定 tag 而非分支）。
- [x] 安装冒烟增加 `pi install` 包路径模式（opt-in，联网）：把 README 推荐的 `pi install git:...@<ref>` 纳入 `tools/install-smoke.mjs`——全新临时 `PI_CODING_AGENT_DIR` 内跑真实 `pi install <source>`，再复用既有 `get_commands` 校验（`parseRpcOutput`/`checkSmoke`），断言装到的包版本、扩展清单与全部命令/TUI 键；避免「推荐安装路径只能人工验证」。（完成 2026-09-11 @ feat/install-package-smoke，根 2.26.0：`--install <源>` 真跑真实 `pi install` → `findPiPackage` 定位落地产物（git 源在 `<configDir>/git`、npm 源在 `<configDir>/npm/node_modules`、本地路径指回源目录）→ 复用 `get_commands` 校验并报告包名@版本/扩展清单/命令/TUI 键；`checkInstallRun` 以退出码 + stdout `Installed <源>` 为判据（实测发现 git 克隆进度写 stderr，不能当失败信号）；新增 5 个纯函数单测（14→19），`checkSmoke` 支持多根来源校验；真机验证：远端 `@dev-laptop` → `pi-agent-extensions@2.24.1` 12 扩展 50 命令 9 键全过、本地路径 → 2.25.0 全过、`--install . --task` 模型经全新安装的 `loop_list` 执行成功；README/AGENTS 同步）
- [ ] 清理孤儿目录 .worktrees/pwr-session-shutdown（84MB，未注册 worktree、无 .git、含文件副本；triage 实测发现，需人工确认后删除）
- [ ] 插件目录收进 `src/`（仓库根布局整理）：把根 `package.json` `pi.extensions` 注册的 13 个插件目录（pwr、agent-team、stream-token-speed、chatanywhere-provider、provider-quota、run-timer、loop、goal、opencode-bridge、deep-init、human-notify、solo-mode、todo-cli）从仓库根移入 `src/<插件名>/`，manifest 路径同步为 `./src/<插件名>/index.ts`；随动面——`tools/install-smoke.mjs` 的目录映射与期望表、`tools/capture-screens.mjs` 及跨插件测试的相对 import（引 pwr `src/ui/viewer.ts` 这类）、各插件 README / `docs/` 卡与 AGENTS.md 的路径与命令、`todos/<插件名>-todo.md` 与插件目录的一一对应（只改路径不改变名字）、根 `package.json` scripts 与各插件 `npm` 工作目录说明；范围只含插件目录（`tools/`、`test/`、`docs/` 不动）。验收：`node tools/todo.mjs lint`、根三条自检（test:contract / test:smoke / test:todo）、各插件全量测试 + typecheck 全绿、`node tools/install-smoke.mjs` 13/13 扩展 + 57 条命令通过、worktree 合并后主干复核正常。（用户 2026-09-11 提出；注意会形成 `src/pwr/src/…` 这类嵌套 src，如需改名（如 `src/pwr/lib/`）开工前先确认）
- [ ] install-smoke 剔除 PI_AGENT_TEAM_* 前缀环境变量（spawnPi 前清理）：team agent 环境内直跑会让 agent-team 进 leader 模式不注册 /team:*，冒烟误报（来源：run-1789100834291 W4/QA 实测，env -u 可绕过）
