{
  "name": "pi-extension-loop",
  "private": true,
  "version": "1.4.0",
  "type": "module",
  "description": "loop — Pi 定时任务扩展（/loop：固定间隔 / 每天定时 / 每日窗口循环 + 一次性提醒 + --bg 后台 agent 模式（可选模型指定）+ agent 工具）",
  "engines": {
    "node": ">=22.18"
  },
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "test": "node --test \"test/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@types/node": "^22.0.0",
    "typebox": "^1.3.10",
    "typescript": "^5.8.0"
  }
}
