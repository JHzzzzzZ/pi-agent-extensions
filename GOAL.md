# GOAL.md — 项目愿景

> 本文件是工作区一切开发工作的最高准绳。每次开工前读一遍，收尾时用 §3 的问题自检。

## 1. 愿景

**把"派 Agent"建成我最喜欢用、最好用的 Agent 框架。**

- 实现路径：**Pi 扩展（extension / 插件）**。所有能力都以插件形态生长——每个插件独立、自包含、可单独启用（`pwr`、`agent-team`、`loop`、`goal`、`human-notify` 等皆循此例）。
- 可扩展方向：后续可以把 speaker、其他形态的 agent 等纳入本框架，统一到同一套"派 Agent"体验下。

## 2. 尺子：怎样算"最好用"

愿景必须可判定，否则收尾自检就是自我打分。两把尺子：

- **自己这把**：我每天真实用它派 agent 干我的活，连续一周没有一次想退回去手动干或换别的工具（pi-subagents 原生体验、Claude Code 等）。想退回去的那一刻，就是最宝贵的改进线索——记下来，变成 `todos/` 条目。
- **别人这把**：目标用户不止我自己——**要给别人用的**。别人拿到手能跑起来、看得懂文档、用得上核心功能。因此安装体验与文档是一等公民：复制安装步骤清晰、README 能独立看懂、真机截图齐全。

## 3. 差异化：和谁比、赢在哪

参照物是 pi-subagents / Claude Code / opencode 等派 agent 工具。本框架的立身之本：

- **自包含零依赖**：对照外部参考"抄改 + 测试锁定"，绝不引入运行时依赖；缺了谁都能跑。
- **完全归我所有**：随时可改、可裁剪、可扩展，不等上游发版。
- **插件自由组合**：每个能力独立启停，用户只装自己要的；装了就互不踩脚。
- **中文优先**：文档、注释、TUI 文案对中文用户第一顺位友好。

与外部对照物冲突时，以上差异化为判据（例如外部改了 TUI，我们"同步细节但不引依赖"）。

## 4. 里程碑

- **M1（近期形态）**：我连续一周用本框架完成全部真实派单工作，全程不需要退出到别的工具；另找 ≥1 位外部用户从零装到跑通核心流程，中途无需我口头救场。
- **M2（中期形态）**：把 speaker 等其他形态的 agent 纳入统一"派 Agent"体验——不同形态的 agent 在同一套派发/观测/控制/对话入口下工作。
- 里程碑达成后在此更新下一个；未达成时 `todos/` 里离 M1 最近的条目优先级最高。

## 5. 每次运行的自检问题（收尾必答）

每次任务结束前，回答一句话并写进收尾报告：

> **这次运行让"最好用的派 Agent 框架"离目标更近了什么？还差什么？**

- 更近了什么：本次改动对体验/可靠性/能力面的具体贡献——最好能落到 §2 的某把尺子上（我会更愿意用它 / 别人更容易用起来）。
- 还差什么：暴露的下一个最大痛点（转成 `todos/` 条目）。
- 答不出"更近了什么"的任务要反省：是不是在给愿景添砖，还是在堆无关的砖。
