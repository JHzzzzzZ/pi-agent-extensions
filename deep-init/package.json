{
  "name": "pi-extension-deep-init",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "deep-init — /deep-init 深度初始化：扫描仓库结构并生成层级 AGENTS.md 项目知识库（update 增量 / --create-new 全量重建）",
  "engines": {
    "node": ">=22.18"
  },
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "test": "node --test \"*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.83.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
